start server:

cd C:\Users\micha\Documents\vedelervfx
python -m http.server

push:

# 1) Make sure you are in your project folder
cd C:\Users\micha\Documents\vedelervfx

# 2) Check what has changed
git status
# → Shows modified / new files

# 3) Add changes you want to commit
git add .
# (or add specific files instead of '.')

# 4) Commit with a clear message
git commit -m "Short description of what changed"

# 5) Pull remote changes first (avoids push errors)
git pull --rebase

# 6) Push your changes to GitHub
git push
